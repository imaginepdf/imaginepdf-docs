---
title: Generate a PDF
description: Render a single PDF from a design, filling its variables, and get a temporary download URL.
---

import { Callout } from 'nextra/components'

# Generate a PDF

```
POST /designs/:designId/generate
```

Renders one PDF from a design, substituting variable values, and returns a
temporary presigned download URL. Costs **1 credit**.

## Path parameters

| Param | Type | Required | Notes |
| ----- | ---- | -------- | ----- |
| `designId` | string | yes | The design id to render. |

## Body

| Field | Type | Required | Notes |
| ----- | ---- | -------- | ----- |
| `data` | object | no | Variable name → value. Omit for a design with no variables. |

```bash
curl -X POST "https://api.imaginepdf.com/api/v1/designs/design_abc123/generate" \
  -H "X-API-Key: $IMAGINEPDF_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "data": {
      "invoice_number": "INV-001",
      "customer_name": "Acme Corp",
      "amount": "$1,000.00"
    }
  }'
```

## Response `201 Created`

```json
{
  "status": "success",
  "data": {
    "designId": "design_abc123",
    "filename": "Invoice.pdf",
    "downloadUrl": "https://storage.imaginepdf.com/....pdf?signature=...",
    "expiresIn": 3600,
    "status": "completed"
  },
  "error": null
}
```

- `downloadUrl` — presigned URL to the rendered PDF.
- `expiresIn` — seconds the URL stays valid (3600 = one hour).
- `filename` — suggested filename, derived from the design name.

Download it:

```bash
curl -L -o invoice.pdf "<downloadUrl>"
```

<Callout type="warning">
  The download URL expires after `expiresIn` seconds. Fetch and store the PDF
  promptly rather than passing the URL around long-term.
</Callout>

## Errors

| HTTP | `error.code` | Meaning |
| ---- | ------------ | ------- |
| 402 | `INSUFFICIENT_CREDITS` | Workspace is out of credits. |
| 404 | `DESIGN_NOT_FOUND` | No design with that id in the workspace. |
| 400 | `VALIDATION_ERROR` | Malformed body. |

See [Errors](/api/errors) for the full table, and
[Credits & plans](/concepts/credits-and-plans) for credit behavior.

## Related

- [Generate a PDF in Node.js with the REST API](https://imaginepdf.com/blog/generate-pdf-nodejs-rest-api)
- [Generate PDFs in Next.js Route Handlers](https://imaginepdf.com/blog/generate-pdf-nextjs)
- [Preview](/api/preview) — render a page to PNG for free while iterating.
- [Batch generation](/api/batch) — one PDF per dataset row.
- [Variables](/concepts/variables) — how `data` maps onto the design.

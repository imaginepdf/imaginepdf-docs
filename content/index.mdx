---
title: Introduction
description: ImaginePDF turns one designed template into PDFs at any volume — through a visual editor, a REST API, an MCP server, or Claude Code.
---

<div className="ip-hero">

# ImaginePDF

<div className="ip-hero-sub">
  The fastest way to generate business documents at scale. Design once, bind
  the parts that change, then produce them by the thousand.
</div>

</div>

<div className="ip-cards">
  <a className="ip-card" href="/quickstart">
    <div className="ip-card-title">Quickstart →</div>
    <div className="ip-card-desc">API key to a downloadable PDF in about five minutes.</div>
  </a>
  <a className="ip-card" href="/concepts/designs">
    <div className="ip-card-title">Concepts →</div>
    <div className="ip-card-desc">Designs, variables and assets — the model behind everything else.</div>
  </a>
  <a className="ip-card" href="/api/overview">
    <div className="ip-card-title">API Reference →</div>
    <div className="ip-card-desc">Every endpoint, with request and response shapes.</div>
  </a>
</div>

Invoices, payslips, receipts, certificates. Design them once in a visual canvas,
bind the parts that change to named fields, then render them one at a time or a
thousand at a time — from the editor, your own code, or an AI assistant.

ImaginePDF does one job: turning a template into PDFs at any volume. There are
no bolt-on AI editors, e-signature flows, or workflow builders to wade through —
just the pieces that make document generation repeatable.

## The model in one minute

<ol className="ip-steps">
  <li>
    <strong>Design</strong> a document on a canvas — text, tables, images,
    shapes, icons, QR codes and barcodes, positioned on one or more pages.
  </li>
  <li>
    <strong>Bind</strong> any element to a named field (a <em>variable</em>), so
    one template covers every customer, every invoice, every certificate.
  </li>
  <li>
    <strong>Generate</strong> — fill the variables with values and render. One
    at a time, or in batch from a dataset.
  </li>
</ol>

A saved document is a **design**. A design plus its variables is everything the
renderer needs to produce a finished PDF.

## Four ways to use it

<div className="ip-cards">
  <a className="ip-card" href="/editor/visual-editor">
    <div className="ip-card-title">Visual editor</div>
    <div className="ip-card-desc">
      The canvas app at imaginepdf.com. The person who owns the brand owns the
      template — no code.
    </div>
  </a>
  <a className="ip-card" href="/api/overview">
    <div className="ip-card-title">REST API</div>
    <div className="ip-card-desc">
      Issue documents from your own product without a human in the loop.
      Authenticated with a workspace API key.
    </div>
  </a>
  <a className="ip-card" href="/mcp/mcp-server">
    <div className="ip-card-title">MCP server</div>
    <div className="ip-card-desc">
      Connect Claude, Cursor or any MCP client and let the assistant design and
      generate directly.
    </div>
  </a>
  <a className="ip-card" href="/plugin/claude-code">
    <div className="ip-card-title">Claude Code plugin</div>
    <div className="ip-card-desc">
      Spin up a one-off PDF — or a batch — without leaving your terminal, using
      the <code>imaginepdf</code> plugin.
    </div>
  </a>
</div>

All four talk to the same backend and produce identical PDFs.

## Where to start

- **New here?** Generate your first PDF with the [Quickstart](/quickstart).
- **Building an integration?** Read [Concepts](/concepts/designs) for designs,
  variables and assets, then jump to the [API Reference](/api/overview).
- **Working with an AI assistant?** Connect the
  [MCP server](/mcp/mcp-server), or install the
  [Claude Code plugin](/plugin/claude-code).
- **Not writing code?** Everything here has a counterpart in the
  [visual editor](/editor/visual-editor).
